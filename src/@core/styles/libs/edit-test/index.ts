import { styled } from '@mui/material/styles'

export const EditTestWrapper = styled('div')(() => ({}))

export const SubmitButton = styled('div')(() => ({
  position: 'fixed',
  bottom: 130,
  right: 130
}))
